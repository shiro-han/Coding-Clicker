//lines of code -- units
import React from 'react';

const Display = (props) => {
    return(
        <div>
            Lines of Code: {props.code}
            Clicks per second: {props
            }
        </div>
    )
}

export default Display;